@model Warehouse.Models.ProductInStorageListModel

<br />
<div class="container row p-0 m-0">
    <div class="col-6">
        <h2 class="text-info">Products</h2>
    </div>

    <div class="col-12 border p-3">
        <table class="table table-striped table-bordered" style="width:100%">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Manufacturer</th>
                    <th>Storage Conditions</th>
                    <th>Pack</th>
                    <th>Expiration Date</th>
                    <th>Receipt Date</th>
                    <th>Supplier Name</th>
                    <th>Supplier Address</th>
                    <th>Supplier Phone</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var asset in @Model.Assets)
                {
                <tr>
                    <td>@asset.Name</td>
                    <td>@asset.Manufacturer</td>
                    <td>@asset.StorageConditions</td>
                    <td>@asset.Pack</td>
                    <td>@asset.ExpirationDate</td>
                    <td>@asset.ReceiptDate</td>
                    <td>@asset.SupplierName</td>
                    <td>@asset.SupplierAddress</td>
                    <td>@asset.SupplierPhone</td>
                </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="d-flex justify-content-end col-12">
        <div class="col-3">
            <a asp-action="Index" class="btn btn-success form-control">Back</a>
        </div>
    </div>
</div>

